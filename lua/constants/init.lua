local ENV = require('constants.env')
local buftypes = require('constants.buftypes')
local filetypes = require('constants.filetypes')

return {
  buftypes = buftypes,
  filetypes = filetypes,
  ENV = ENV,
}
